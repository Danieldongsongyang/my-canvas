# 桌面端不承载管理后台

桌面端不保留当前项目的 `/admin/*` 管理后台，也不把这些页面强行改造成 `mange-backend` 管理后台的替代品。账号注册、模型渠道添加、用户管理、Key 管理和后台设置继续由 `mange-backend` 网页端承载，桌面端只在需要时打开或跳转到该网页端。

**Consequences**

当前前端的旧 `/api/admin/*`、旧 admin 页面和旧后台协议可以从桌面客户端范围中移除。`mange-backend` 网页端必须保留并保持独立，因为它是后续模型配置、用户登录相关网页流程和后台管理的真实入口。
