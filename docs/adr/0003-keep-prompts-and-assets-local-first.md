# 提示词和素材库第一阶段本地化

桌面端第一阶段不为提示词库和素材库新增 `mange-backend` 接口，也不把当前项目旧的 `/api/prompts`、`/api/assets` 协议迁移到后端。内置提示词可改为前端静态数据，用户素材继续由当前前端的本地持久化能力承载。

**Consequences**

`mange-backend` 继续专注账号、额度、模型渠道和 AI relay，不承担画布素材与提示词业务。后续如果需要云同步或团队共享，再单独设计后端数据模型和同步协议。
