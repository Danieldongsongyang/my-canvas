# 第一阶段只支持用户名密码登录

桌面端第一阶段只实现用户名密码登录。注册、OAuth、Passkey、邮箱验证、人机验证等复杂流程继续由 `mange-backend` 网页端承载；如果后端返回需要 2FA，桌面端先给出清晰提示，后续再单独支持 `/api/user/login/2fa`。

**Consequences**

登录页保持简单，优先打通桌面端主闭环。后续增强登录体验时，需要在不暴露 relay API Key、不复刻完整后端网页端的前提下设计 2FA 或网页登录回跳。
